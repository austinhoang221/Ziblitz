export interface IProject {
    id: string,
    name: string,
    code: string,
    description: string,
    createDate: string,
    leaderId: string,
    avatarUrl: string,
    isFavorite: boolean,
}